// src/components/SuperMarket.js
import React from 'react';
import { SuperMarketTemplate } from '../components';

const SuperMarket = () => {
  return (
    <SuperMarketTemplate 
      items={null} 
      basket={null} 
    />
  );
}

export default SuperMarket;